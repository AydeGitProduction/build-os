{
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/orchestrate/cron",
      "schedule": "*/5 * * * *"
    }
  ],
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_AUTHOR_LOGIN\" = \"build-os-agent[bot]\" ]; then echo 'Skipping deploy: build-os-agent[bot] commit — agents deploy via explicit Vercel API only'; exit 1; fi; exit 0"
}
